CREATE TABLE decorator
(
    id int AUTO_INCREMENT PRIMARY KEY,
    uname varchar(20),
    name text,
    address text,
    contact1 bigint,
    contact2 bigint,
    FOREIGN KEY (uname) REFERENCES providers(uname)
);
ALTER TABLE decorator ADD COLUMN price int;