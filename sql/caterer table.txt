CREATE TABLE caterer
(
    id int AUTO_INCREMENT PRIMARY KEY,
    uname varchar(20),
    name text,
    address text,
    contact1 bigint,
    contact2 bigint
);
ALTER TABLE caterer ADD FOREIGN KEY (uname) REFERENCES providers(uname);
ALTER TABLE caterer ADD COLUMN price int;